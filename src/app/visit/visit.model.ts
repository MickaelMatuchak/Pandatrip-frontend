export class VisitModel {
  constructor(id: number, name: string, address: string) {
    this.id = id;
    this.name = name;
    this.address = address;
  }
  id: number;
  name: string;
  address: string;
}
